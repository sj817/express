{
  "name": "@karinjs/router",
  "description": "Simple middleware-style router",
  "version": "2.2.0",
  "author": "Douglas Christopher Wilson <doug@somethingdoug.com>",
  "contributors": [
    "Blake Embrey <hello@blakeembrey.com>"
  ],
  "license": "MIT",
  "repository": "sj817/express",
  "homepage": "https://github.com/sj817/express",
  "type": "module",
  "dependencies": {
    "debug": "catalog:",
    "parseurl": "catalog:",
    "path-to-regexp": "workspace:*"
  },
  "devDependencies": {
    "@karinjs/express": "workspace:*",
    "@types/finalhandler": "^1.2.4",
    "@types/mocha": "^10.0.10",
    "qs": "catalog:",
    "finalhandler": "^2.1.0",
    "mocha": "10.2.0",
    "nyc": "15.1.0",
    "run-series": "^1.1.9",
    "standard": "^17.1.0",
    "supertest": "6.3.3"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">= 18"
  },
  "scripts": {
    "build": "tsc && tsdown",
    "lint": "standard",
    "test": "mocha --reporter spec --bail --check-leaks test/",
    "test:ts": "mocha -r tsx --reporter spec --bail --check-leaks test/*.ts",
    "test:debug": "mocha --reporter spec --bail --check-leaks test/ --inspect --inspect-brk",
    "test-ci": "nyc --reporter=lcov --reporter=text npm test",
    "test-cov": "nyc --reporter=text npm test",
    "version": "node scripts/version-history.js && git add HISTORY.md"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.mjs"
    }
  }
}